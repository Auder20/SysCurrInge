import React from "react";

function EditUser() {
  console.log("🚀 EditUser: Componente simplificado montado!");
  
  return (
    <div>
      <h1>Editar Usuario (Versión Simplificada)</h1>
      <p>Si ves esto, el componente funciona.</p>
    </div>
  );
}

export default EditUser;
